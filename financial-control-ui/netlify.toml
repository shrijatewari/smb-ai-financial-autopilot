# Netlify: use when this folder is the site root (Git base dir = financial-control-ui)
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://smb-financial-api.fly.dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
